kind = "web"
previewPath = "/"
title = "infinity-ai Voice Assistant"
version = "1.0.0"
id = "artifacts/infinity-ai"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21662

[services.development]
run = "pnpm --filter @workspace/infinity-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/infinity-ai", "run", "build" ]
serve = "static"
publicDir = "artifacts/infinity-ai/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21662"
BASE_PATH = "/"
