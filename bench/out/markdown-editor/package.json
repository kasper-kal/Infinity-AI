{
  "name": "markdown-editor",
  "version": "1.0.0",
  "private": true,
  "description": "Markleaf — markdown editor (Good Gate app #5)",
  "type": "module",
  "scripts": {
    "build": "esbuild src/editor.js --bundle --minify --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}