{
  "name": "saas-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Lantern — SaaS marketing landing page (Good Gate app #1)",
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --outfile=dist/bundle.js"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}